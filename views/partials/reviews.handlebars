<p class="font-bold text-2xl mb-2 text-dark-blue">User Reviews:</p>
<div class="reviews">
  {{#each this}}
  <section class="review">
    <div>
      <p class="font-bold text-xl mb-2 text-dark-blue">{{user.username}}'s opinion:</p>
    </div>
    <div class="text font-bold text-md mb-2 text-blue">
      {{review_text}}
    </div>
  </section>
  {{/each}}
</div>