<!DOCTYPE html>
<html lang="en">

<head>
   <meta charset="UTF-8">
   <meta http-equiv="X-UA-Compatible" content="IE=edge">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <link href="/stylesheets/style.css" rel="stylesheet">
   <title>TAPP THAT.</title>
</head>

<body>

   <header class="h-14 py-15 text-left bg-drinks"></header>
      <a class="text-dark-grey text-6xl pt-8 pb-8 font-sans " href="/">TAPP THAT.</a>
   </header>

   <div class="pt-3">
      <nav class="text-right pb-6">
         {{#if loggedIn}}
         <a class="text-dark-grey border border-light-grey hover:bg-dark-grey hover:text-light-grey bg-tan font-bold uppercase px-4 py-1 rounded outline-none focus:outline-none mr-1 mb-1 ease-linear transition-all duration-150"
            href="/">Home</a>
         <a class="text-dark-grey border border-light-grey hover:bg-dark-grey hover:text-light-grey bg-tan font-bold uppercase px-4 py-1 rounded outline-none focus:outline-none mr-1 mb-1 ease-linear transition-all duration-150"
            href="/mybar">My Bar</a>
         <a id="logout-btn"
            class="text-dark-grey border border-light-grey hover:bg-dark-grey hover:text-light-grey bg-tan font-bold uppercase px-4 py-1 rounded outline-none focus:outline-none mr-1 mb-1 ease-linear transition-all duration-150"
            href="/login">Logout</a>
         {{else}}
         <a class="text-dark-grey border border-light-grey hover:bg-dark-grey hover:text-light-grey bg-tan font-bold uppercase px-4 py-1 rounded outline-none focus:outline-none mr-1 mb-1 ease-linear transition-all duration-150"
            href="/login">Login</a>
         <a class="text-dark-grey border border-light-grey hover:bg-dark-grey hover:text-light-grey bg-tan font-bold uppercase px-4 py-1 rounded outline-none focus:outline-none mr-1 mb-1 ease-linear transition-all duration-150"
            href="/signup">Sign Up</a>
         {{/if}}
      </nav>
   </div>

   <main>
      {{{ body }}}
   </main>

   <footer class="text-sans text-center pl-5 fixed bottom-5 text-salmon">
      Drink responsibly. Post liberally.
   </footer>


   {{#if loggedIn}}
   <script src="/javascript/logout.js"></script>
   {{/if}}
</body>

</html>